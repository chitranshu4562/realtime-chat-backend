export const SOCKET_EVENTS = {
    // ── System ────────────────────────────────────────────────────────────
    SYSTEM: {
        CONNECT: 'connection',
        DISCONNECT: 'disconnect',
        ERROR: 'error',
    },
} as const;